# راهنمای نصب روی آیفون و اندروید (PWA)

با این تغییرات، از روی مرورگر گوشی می‌تونید اپ رو با یه ضربه به صفحه‌ی اصلی اضافه کنید — دقیقاً شبیه یه اپ واقعی باز میشه، بدون نیاز به Mac/Xcode، Google Play، یا هزینه‌ای. **هم روی آیفون هم اندروید کار می‌کنه.**

## چی جدید / تغییر کرده؟

**فایل‌ها و پوشه‌های کاملاً جدید (فقط اضافه میشن):**
- `pwa-assets/manifest.json`
- `pwa-assets/icons/apple-touch-icon.png`, `icon-192.png`, `icon-512.png`
- `pwa-assets/sw.js` ← service worker؛ برای اینکه روی اندروید هم دکمه‌ی نصب واقعی (نه فقط bookmark) نشون داده بشه لازمه
- `.github/workflows/deploy-pwa.yml`
- `vite.config.pages.ts` ← فایل تنظیمات **جدا** فقط برای build وب

**فایل‌های ویرایش‌شده (تغییرات کوچیک):**
- `index.html` — چند خط `<link>`/`<meta>` + یک اسکریپت کوتاه ثبت service worker
- `package.json` — یک خط: `"build:pwa": "vite build --config vite.config.pages.ts",`

**دست‌نخورده (برای build ویندوز خیالتون راحت باشه):**
- `vite.config.ts` — بدون کوچیک‌ترین تغییر، همون نسخه‌ای که قبلاً موفق build شد
- هیچ‌کدوم از فایل‌های `pwa-assets/` وارد build ویندوز نمیشن (عمداً جداشون کردم، نه توی پوشه‌ی `public/` مشترک)

## مراحل انتشار

۱. فایل‌های بالا رو (دقیقاً مثل قبل) روی همون ریپازیتوری گیت‌هاب commit کنید.

۲. توی ریپازیتوری، Settings ← Pages. زیر «Source»، **«GitHub Actions»** رو انتخاب کنید (یک‌بار لازمه).

۳. برید تب Actions — workflow «deploy-pwa» رو ببینید که شروع شده (یا دستی از Run workflow اجراش کنید). ۱ تا ۲ دقیقه طول می‌کشه.

۴. بعد از سبز شدن، از Settings ← Pages آدرس رو بردارید: `https://<username>.github.io/<repo-name>/`

## نصب روی آیفون

توی **Safari** آدرس رو باز کنید ← آیکون Share ← **Add to Home Screen**.

## نصب روی اندروید

توی **Chrome** آدرس رو باز کنید. یا خودش یه بنر/پیشنهاد **«Install app»** پایین صفحه نشون می‌ده، یا از منوی سه‌نقطه‌ی بالای Chrome، گزینه‌ی **«Install app»** (یا «Add to Home screen») رو بزنید. تفاوتش با iOS اینه که روی اندروید این کار واقعاً اپ رو مثل یه اپ نصب‌شده به لیست اپ‌ها هم اضافه می‌کنه، نه فقط آیکون روی Home Screen.

## نکات

- ریپازیتوری Public هست، پس آدرس از نظر فنی برای هرکسی با لینک قابل بازدیده — ولی دیتای واقعی (نمرات، حضور و غیاب) هیچ‌وقت وارد این سایت نمیشه، چون همیشه فقط داخل مرورگر خود کاربر ذخیره میشه.
- service worker طوری نوشته شده که همیشه اول نسخه‌ی آنلاین/تازه رو نشون بده؛ فقط وقتی اینترنت نباشه از نسخه‌ی ذخیره‌شده استفاده می‌کنه — یعنی بعد از هر آپدیت، با اولین باز کردن (با اینترنت) نسخه‌ی جدید رو می‌بینید، نگران "نسخه‌ی قدیمی گیر کردن" نباشید.
- هر آپدیت بعدی: commit کنید، هر دو workflow (`build-windows-exe` و `deploy-pwa`) جدا و مستقل اجرا میشن.

