# راهنمای ساخت فایل نصبی ویندوز (exe)

این پوشه یک پروژه‌ی [Tauri](https://tauri.app) کامل و آماده است. اپ وب شما بدون هیچ تغییری همون‌جوری که هست کنار یک لایه‌ی Rust خیلی سبک قرار گرفته که فقط WebView2 (که در ویندوز ۱۰/۱۱ از قبل نصبه) رو باز می‌کنه — برای همین برخلاف Electron نیازی به بسته‌بندی یک مرورگر کامل نیست.

## پیش‌نیازها (فقط یک‌بار، روی همون سیستمی که build می‌گیرید)

1. **Node.js** — اگر از قبل ندارید، نسخه‌ی LTS رو از [nodejs.org](https://nodejs.org) نصب کنید.
2. **Rust** — از [rust-lang.org/tools/install](https://www.rust-lang.org/tools/install) فایل `rustup-init.exe` رو دانلود و با تنظیمات پیش‌فرض نصب کنید.
   - اگر در حین نصب گفت که به **Microsoft C++ Build Tools** نیاز داره، لینکی که نشون می‌ده رو باز کنید و گزینه‌ی **"Desktop development with C++"** رو نصب کنید. این مرحله چند دقیقه طول می‌کشه ولی فقط یک بار لازمه.

## مراحل ساخت exe

توی پوشه‌ی پروژه یک ترمینال (PowerShell یا CMD) باز کنید و به‌ترتیب:

```bash
npm install
npm run tauri build
```

- `npm install` وابستگی‌های جاوااسکریپت + ابزار خط‌فرمان Tauri رو نصب می‌کنه (چند دقیقه).
- `npm run tauri build` هم اپ React رو build می‌کنه و هم فایل exe نهایی رو می‌سازه. **بار اول** چون Rust باید کامپایل بشه ممکنه ۵ تا ۱۵ دقیقه طول بکشه؛ دفعات بعدی خیلی سریع‌تره.

فایل نصبی نهایی اینجا قرار می‌گیره:

```
src-tauri/target/release/bundle/nsis/وب آموزگار_0.1.0_x64-setup.exe
```

همین فایل رو می‌تونید روی هر سیستم ویندوزی اجرا کنید — یک ویزارد نصب کامل باز می‌شه (فارسی/انگلیسی)، برنامه رو توی Program Files نصب می‌کنه، شورتکات دسکتاپ/Start Menu می‌سازه و توی «Add or Remove Programs» هم قابل حذفه.

## تست سریع قبل از build نهایی (اختیاری)

اگه می‌خواید قبل از ساخت فایل نصبی، اپ رو به‌صورت پنجره‌ی دسکتاپ تست کنید:

```bash
npm run tauri dev
```

این حالت توسعه‌ست، با hot-reload، ولی exe نهایی نمی‌سازه.

## نکات

- چون این exe امضای دیجیتال (code-signing certificate) نداره، دفعه‌ی اول که روی یک سیستم دیگه اجراش کنید، ویندوز SmartScreen ممکنه هشدار بده. با زدن **More info → Run anyway** رد می‌شه؛ این طبیعیه و نشونه‌ی مشکل نیست.
- داده‌های اپ (دانش‌آموزان، نمرات، حضور و غیاب و…) همون‌طور که در نسخه‌ی وب بود، محلی و داخل خود اپ ذخیره می‌شن — چیزی به سروری ارسال نمی‌شه.
- اگه خواستید بعداً هشدار SmartScreen کلاً حذف بشه، به یک گواهی امضای کد (code-signing certificate) نیاز دارید که تهیه‌ش جداست و اختیاریه.
